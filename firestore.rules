rules_version = '2';

// 這個網站的所有資料讀寫都透過伺服器端的 Firebase Admin SDK 進行，
// Admin SDK 會略過這些規則。因此前端（瀏覽器）一律拒絕存取，最安全。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
